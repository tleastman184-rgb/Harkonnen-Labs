target/
.env

# SQLite state — regenerated at runtime
*.db
*.db-shm
*.db-wal

# Generated indexes and caches — rebuilt by `harkonnen memory init`
factory/memory/index.json
**/project-memory/index.json

# Factory runtime directories — not source
factory/logs/
factory/artifacts/
factory/workspaces/
products/

# Hugging Face model cache — downloaded by fastembed on first run (~33MB)
# Lives at ~/.cache/huggingface/hub/ outside the repo; listed here as documentation.
# If you vendor the model locally, add its path here too.
